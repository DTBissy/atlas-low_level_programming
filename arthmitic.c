#include <stdio.h>

int main ()
{
	int num1;
	int num2;
	printf("Enter first number: ");
	scanf("%d", &num1);
	printf("ENter second number: ");
	scanf("%d", &num2);

	printf("Awnser is %d", num1 % num2);
		
		return (0);
}
