#include <stdio.h>

int main(void)
{
	int a;
	int b;

	a = 100;
	b = 400;

	if (a == 100) {
		if (b == 400) {
			printf("100 is a and b is 400 or sum\n");
		}
	}
	printf("A's value is : %d\n", a );
	printf("B's value is : %d\n", b );

	return (0);
}
